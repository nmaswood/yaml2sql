require "spec_helper"

RSpec.describe Yaml2sql do
  it "has a version number" do
    expect(Yaml2sql::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
